@{
  Layout = "_Layout";
}
@using System.Security.Claims
@using Market.Models;

  <b><h1>Welcome to my Online Market!</h1></b>
<br>

@if (User.Identity.IsAuthenticated)
{
  
      @Html.ActionLink("Treats", "Index", "Treats")
  <br>
      @Html.ActionLink("Flavors", "Index", "Flavors")
  <br>
  <br>
      @using (Html.BeginForm("LogOff", "Account"))
     {
        <input type="submit" value="Log out" />
     }

}
else
{
  <h3>@Html.ActionLink("Create an account", "Index", "Account")</h3>
  <h3>@Html.ActionLink("Log in", "Login", "Account")</h3>
}


